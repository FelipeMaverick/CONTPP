services:
  - type: web
    name: contratos-produtora
    runtime: python3
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn server:app
    envVars:
      - key: PORT
        value: 10000
